# Bot-specific deps (Phase 01 + 02). Phase 03+ adds gate validator + 11-gate logic.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0

# Telegram dispatcher (Phase 02). Mocked via FakeTelegramTransport in tests.
python-telegram-bot>=21.0,<23.0